import { PluginManifest } from "@/pluginTypes";
import { Entry } from ".";

const manifest: PluginManifest = {
  plugin: "care-scribe",
  routes: {},
  extends: [],
  components: {

  },
  Entry: <Entry/>,
  navItems: [],
};

export default manifest;
